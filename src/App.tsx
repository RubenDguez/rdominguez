import { Box } from '@mui/material';
import { Main } from './layout/Main';

export const App = () => {
    return (
        <Box sx={{ m: 3 }}>
            <Main />
        </Box>
    );
};

export default App;
